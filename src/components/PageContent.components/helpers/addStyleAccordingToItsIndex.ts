
const addStyleAccordingToItsIndex = (index:number)=>{
    const position:number = index + 1;
    const gridColumn = {
      gridColumn: position
    }
    
    return gridColumn
};

export {
    addStyleAccordingToItsIndex
}